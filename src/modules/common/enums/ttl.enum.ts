export enum TTL {
  ONE_SEC = 1,
  TEN_SEC = 10,
  ONE_MIN = 60,
  TEN_MIN = 10 * 60,
  ONE_HOUR = 60 * 60,
  ONE_MIN_IN_MILLISECONDS = 60 * 1000,
  TEN_MIN_IN_MILLISECONDS = 10 * 60 * 1000,
  ONE_HOUR_IN_MILLISECONDS = 60 * 60 * 1000,
}
